# Memory Index

- ["flycam" tag's three silent drops](project_flycam_tag_silent_semantics.md) — tag discards the prefab's own GLB model, last flycam entity wins, player beats flycam — all warn-free; Prop vs Actor has zero runtime divergence (verified from pixels)
- [Browser pixel-probe recipe](project_browser_pixel_probe_recipe.md) — committed pkg/ + serve.py renders RON edits with no rebuild, but headless Chromium has NO WebGPU adapter here (fake "blank screen"); use headless=False + --enable-unsafe-webgpu
- [Gamepad index routing & shared-index double-fire](project_gamepad_index_routing.md) — resolve_gamepad is positional not identity-stable (disconnect re-indexes everyone); shared Some(n) double-fires and the action-bar collision checks now *depend* on that unvalidated premise
- [Test harness latches keyboard just_pressed](project_test_harness_just_pressed_latch.md) — setup_test_app omits InputPlugin so just_pressed never clears; release() alone doesn't help, need clear_just_pressed; silently makes multi-update keyboard tests vacuous (gamepads unaffected)

- [bevy_ui always composites above Text2d/Mesh2d](project_ui_vs_text2d_compositing.md) — verified from pixels + Bevy 0.18 sources; IsDefaultUiCamera stays redundant, "nameplate over HUD" is overlap/translucency, never render order
- [WebGPU preprocessing warnings are red herrings](project_webgpu_preprocessing_warning.md) — Bevy 0.18 "preprocessing are limited" = PreprocessingOnly (still on GPU), "pipeline wasn't ready" is warn_once at startup; neither is per-frame CPU cost
- [world_label_screen_pos_system write guards](project_changedetection_transform_writes.md) — translation/font_size/scale/Visibility are all epsilon-guarded now (fixed); NameplateCameraDistance is the one deliberate unguarded write — a new unguarded write there is a regression
- [label_depth_scale: 2 mechanisms + screen_offset](project_label_depth_scale_three_mechanisms.md) — one all-or-nothing block driving font-size (re-rasterized, crisp) vs. anchor Transform.scale (resampled, blurry Text2d); screen_offset px/m is viewport-height-calibrated; default reference_distance 50.0 never engages
- [Composite "sign-dependent position" is a non-bug](project_composite_sign_position_nonbug.md) — transform/rapier math is sign-symmetric; opposite-sign XZ symptom can't come from composition; real defects = collider replacement (trigger_zone ball lost to compound) + nested RigidBody::Fixed
- [UI click-blocking = coverage + z-order, not Button presence](project_ui_pick_blocking.md) — Bevy 0.18 UI nodes block lower picks by default but only over the rect they cover; non-full-screen panel + GlobalZIndex(51) doesn't block buttons outside it; targeting bypasses UI picking entirely and needs a panel_open guard
- [FocusPolicy::Block required for UI blockers](project_focuspolicy_block_required.md) — ui_focus_system (Interaction path) does NOT block on z-index alone; Node defaults FocusPolicy=Pass, so a blocker needs explicit FocusPolicy::Block or clicks pass through to buttons beneath
- [target_indicator_system double-despawn](project_target_indicator_double_despawn.md) — two loops over one `existing` query snapshot + deferred despawn + no dedup = ring despawned twice ("generation 1" warning) when target dies same frame owner retargets; pre-existing, fix with HashSet dedup or try_despawn
- [Double-despawn bugs are untestable by ECS state](project_idempotent_despawn_untestable_by_state.md) — deferred despawn is idempotent (warn only, no panic, no state change); a regression test asserting entity count/no-panic passes with the bug present; needs log capture to detect
- [stat_display change-detection guards & .as_mut()](project_stat_display_changedetection_asmut.md) — a per-frame guard is defeated if you .as_mut()/get_mut before the value comparison; DerefMut marks Changed regardless (world_icon_bar_update_system does this; world_pixel_bar_update_system reads immutably first, correctly)
- [assemble_player_config Primitive panic](project_assemble_player_config_primitive_panic.md) — spawn_player_entity_core's Primitive arm .expect()s a ctx that only scene-load supplies; Action::Spawn on a primitive player prefab with a non-empty resolvable model key bypasses the empty-model rejection and crashes; model=="" for Primitive is convention, not schema-enforced
- [LevelEntity recursive-sweep double-despawn](project_levelentity_recursive_sweep_double_despawn.md) — scene_loader level sweep despawns LevelEntity parents AND their LevelEntity children (nameplate/Pixel-stat-bar anchors) with recursive despawn(); parent-iterated-first double-despawns the child → "generation N" warn on portal transitions; pre-existing, benign, fix with try_despawn()
- [Gamepad join: emission budget != capture budget](project_gamepad_join_emit_vs_capture.md) — unclaimed_gamepad_trigger_system writes one UiEvent per (pad × binding) match but captures only one pad; interpreter never dedups, so N simultaneous presses = N JoinPlayer actions with only the first pad-bound; side-channel-only tests pass anyway
- [RenderLayers reserved-layer scheme fragility](project_renderlayers_reserved_scheme.md) — `1 + player_index % MAX_SPLIT_PLAYERS` at 4 sites + a hardcoded party-camera union; unvalidated authored player_index silently voids own_viewport_only; spawn-time-only insertion
- [test_web.py missing baseline skips its own checks](project_test_web_missing_baseline_skips_checks.md) — auto-creates the baseline and returns *before* the console-error assert, so a brand-new scene always "passes" unverified (room6/room8/room9 are in this state)
- [camera_mode vs legacy camera dual source](project_camera_mode_dual_source.md) — 2+-player camera dispatch reads PlayerConfig.camera (always defaulted), never camera_mode; migrating a co-op prefab to camera_mode silently resets tuning to engine defaults
- [Unregistered systems are silent](project_unregistered_systems_silent.md) — a new `pub fn *_system` in a pub capabilities module gets no dead_code warning if never added to lib.rs; grep lib.rs for every new system name
- [Camera mode switching drops spawn-only state](project_camera_mode_switch_spawn_only_state.md) — Fixed's `position`, Flycam's yaw/pitch and CameraTargets are applied once at spawn and never recomputed per frame, so a marker+ActiveCameraMode swap silently loses them; also check every camera spawn site inserts AuthoredCameraMode
- [MouseWheel unit reality per platform](project_mousewheel_unit_platform_reality.md) — native Windows Line is always ±1.0/notch (OS lines setting never read); web = browser deltaY × devicePixelRatio (Chrome Pixel ~100-200, Firefox Line 3), so browser-reported wheel bugs are the Pixel path
- [dt-scaled discrete impulses jump 15x](project_dt_scaled_discrete_impulse.md) — Time<Virtual> max_delta=250ms + batched events on the same hitch frame; per-event bounds don't bound the frame total, and fixed-16ms tests can't see either
- [LoadScene teardown is atomic](project_loadscene_teardown_atomicity.md) — Action::LoadScene despawns nothing; teardown is one fresh-query flush in spawn_scene_v2, fires once even for two-LoadScene-same-frame; tracked+dependent entities die in same flush so nameplate/target-indicator cross-system races are self-protected; a bare scene can't produce the despawn warning — look at an earlier-traversed scene
- [Test harness message buffers never rotate](project_test_harness_message_buffers_never_rotate.md) — init_resource::<Messages<T>>() before GamePlugin makes add_message a no-op, so iter_current_update_messages() is cumulative-forever in every integration test
- [FixedUpdate 64Hz vs Rapier Variable 1/60 in PostUpdate](project_fixedupdate_vs_rapier_clock.md) — two clocks; below 60 FPS ticks outrun physics time, so tick-counted windows are framerate-coupled; TimestepMode::Fixed in a test hides it
- [NaN from unclamped RON numerics](project_nan_from_unclamped_ron_numerics.md) — `x <= threshold` is false for NaN so validators go silent; NaN.max(0.0)==0.0, NaN as u16==0; Dir3 panics only in debug, release goes silently NaN
- [is_grounded feeds 3 consumers, not 1](project_is_grounded_overloaded_three_consumers.md) — can_jump's branch reads the coyote-buffered value, so coyote lengthens the double-jump dead window (141→234ms) and a large coyote_time_secs silently disables double jump; reset reads raw_grounded
- [player_slope_jump_tests harness gotchas](project_slope_jump_test_harness_gotchas.md) — Move climbs off the 60m slab onto a walkable 30deg end cap at tick ~215 (200-tick loops have 7% margin); settle loop never runs the movement system so coyote_ticks_remaining is always 0 at first step()
- [Ground cast sees sensors](project_ground_cast_sees_sensors.md) — FIXED on feature/uphill-jump-lock (mutation-verified); a sensor can never be a floor, so exclusion is free; solid-wall veto still open
- [Coyote time has a real upper bound](project_coyote_time_upper_bound.md) — coyote_time_secs >= ~0.8s kills the airborne animation and the jump_exit landing event for a whole jump, despite code comments claiming any non-negative value is valid
