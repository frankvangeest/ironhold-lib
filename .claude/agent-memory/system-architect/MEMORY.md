# Memory Index — system-architect

- [Core architectural decisions](arch_decisions.md) — three-crate split, Message→Action pipeline, ActionQueue FIFO, asset catalog pattern, schema as designer API
- [Fragile modules](fragile_modules.md) — composite prefab spawning, `children:`-on-a-player traps (material flattening, child physics joins player collider), EffectDef/LayerDef sync, WebGPU alignment, particle warmup, terrain async, spawn queue cap; conditional-feature sibling divergence now RESOLVED via shared `attach_prefab_features`
- [Deferred despawn double-queue](deferred_despawn_double_queue.md) — snapshot-iterated-twice + deferred despawn = double despawn class; Bevy 0.18 despawn() warns not panics; try_despawn silences; all known sibling instances (incl. nameplate.rs) now converted to try_despawn()
- [Capability patterns](capability_patterns.md) — how to add capabilities/actions/events, rules.ron vs state_machine.ron, schema stability rules, physics/inspector constraints; the substitution-enumeration trap is real but its inventory-action instance is now fixed
- [WASM pitfalls](wasm_pitfalls.md) — pipeline compilation latency, no threading, 16-byte alignment, binary size limit, asset preloading
- [Scene/prefab boundary](scene_prefab_boundary.md) — how scene vs prefab responsibilities diverge from engine canon; `stat_overrides` now a partial per-instance override channel; broader override gap (material/motion/etc.) still open; recommended direction
- [GPU physics / wgrapier](gpu_physics_wgrapier.md) — wgrapier is WIP/throwaway prototype, no Bevy bridge; CPU height-array is correct for terrain ground queries; defer GPU physics
- [Player spawn paths](player_spawn_paths.md) — player-construction sites (now 5, incl. hot-join); shared-helper rule; executor-side tag detection; dual-camera/tonemapping/terrain-timing caveats; large accreting history file, inline line-number citations may be stale (candidate for future consolidation)
- [Render-only reactive capabilities](render_only_reactive_capabilities.md) — cosmetic capabilities (target_indicator) react to state without ActionQueue; target_indicator now tracks per-player PlayerTarget, not the shared CurrentTarget; intentionally skip tag_spawned_entity; don't flag as violations
- [Player marker gap](player_marker_gap.md) — no dedicated Player marker; CharacterController = de-facto "is local player" across 6 capabilities; add Player{ownership} as MP forward-compat hook
- [Nameplate gating](nameplate_gating.md) — should_insert_nameplate is the shipped single source of truth for all 6 spawn sites; player/NPC show-source split + faction-filter player-bypass + Action::ToggleOwnNameplate all implemented as recommended
- [Shader resolution pattern](shader_resolution_pattern.md) — engine-owned shaders embed via include_str!+UUID handle (CUSTOM_MATERIAL_FALLBACK_HANDLE); designer shaders resolve through catalog; don't hardcode ShaderRef literals; the former stat_radar/foliage/flame_material/particle_renderer literal-path anti-pattern and the foliage fabricated-texture-path footgun are both fixed
- [Time & animation advance](time_and_animation_advance.md) — Bevy (not our system) advances AnimationPlayer off Time<Virtual>; freeze/pause/slow features act on the clock, not by skipping our systems; harness modes go URL-param→start_app→Resource
- [Determinism & networking](determinism_networking.md) — static mode is orthogonal to net determinism; Rapier cross-platform float divergence is the hard blocker; recommend SimClock chokepoint + run-mode enum
- [Update vs FixedUpdate schedule](schedule_update_vs_fixedupdate.md) — interpreter chain + action_executor in Update; NPC/physics AI in FixedUpdate; cross-schedule GameEvent timing hazard + combat damage flow
- [NPC state machine design](npc_state_machine_design.md) — NpcState is runtime-only (not schema); Investigating state (split from Chase) is SHIPPED, dist_opt.or hack is gone; centralize hit-handling
- [Camera architecture](camera_architecture.md) — camera_modes v1+v2, Update chain, 8 spawn sites, fov 45→60 trap, flycam tag predicates + Action::Spawn flycam blind spot
- [Camera pose-writer taxonomy](camera_pose_writer_taxonomy.md) — absolute-pose (Orbit/Party/FirstPerson) vs accumulator (Follow/Flycam/Fixed) mode systems; the precondition behind shake filtering, blend soundness, and switch correctness
- [Dialogue system](dialogue_system.md) — catalog-vs-structural-file rule; auto-wire seam; dual condition system; auto-advance-only-when-no-choices invariant; portrait must use catalog
- [UI hover & tooltip](ui_hover_and_tooltip.md) — Interaction needs Button; inventory slots lack it; cursor/viewport via PrimaryWindow (WASM-safe); UI Camera2d order 1000 + GlobalZIndex; ActionSlotDef.label is unrendered "future use"
- [World-space widgets](world_space_widgets.md) — per-prefab (stat_label/world_stat_bar) vs scene-managed (nameplate); share WorldPixelBarFillMarker primitive, keep systems separate; partial not full consolidation; nameplate anchor leak now fixed via RemovedComponents<NameplateTag> cleanup, predicate copy-paste now fixed via should_insert_nameplate; two-writer Visibility hazard still open; extensive later split-screen/depth-scale/style-taxonomy history (Icon/Textured bars, screen_offset stacking)
- [Bevy 0.19 upgrade](bevy_019_upgrade.md) — wait/don't-upgrade-now; gated on rapier+framepace+bevy_common_assets 0.19; corrected audit (clear_entities/AnimationTargetId/Ref/Skybox N/A); ~5-7d; silent WebGPU+size risks
- [RNG & determinism](rng_and_determinism.md) — seed RNG in core (ChaCha8Rng) from day one, never thread_rng; getrandom js only needed by the self-seeding path; Beta 0.5 needs seeded RNG anyway
- [Event pipeline & intent layer](event_pipeline_intent_layer.md) — pipeline IS intent/exec/result; intent layer SHIPPED (action_bar routes through it); per-slot loop (no longer one-slot-per-frame) and per-player cost gate both fixed; defer 4-phase combat; CLI event validator for typo risk
- [Animation seek/freeze constraints](animation_seek_freeze_constraints.md) — bevy_animation 0.18 facts: paused clip is never faded out (permanent blend bug), seek applies only on clip change, must be durable for WASM GLTF respawn
- [Spawn-frame visibility accounting](spawn_frame_visibility_accounting.md) — SpawnScene runs after Update so "hide after spawn" is always ≥1 rendered frame too late; animate_targets writes nothing the frame a graph is created
- [Animation two-stage pipeline](animation_two_stage_pipeline.md) — resolver (single writer of .current, owns stop_action sentinel) vs animation.rs (graph build/play); npc_revive sentinel leak FIXED via generalized is_stop_sentinel check
- [Audio mute/volume state machine](audio_mute_state_machine.md) — AudioState.muted is single source of truth; bind:audio_state label is pure read projection driven only by executor-emitted audio.muted/unmuted; ui. prefix stripped from button action
- [Guard vs behavior distinction](guard_vs_behavior_distinction.md) — emission guards (panel_open) belong in Rust not RON; LogicState is a scalar mode-slot not an orthogonal flag; hybrid = keep bool + emit ui.panel_opened/closed events
- [Camera-selection primitive shapes](camera_selection_primitive_shapes.md) — split-screen camera selection is 3 distinct shapes (v1 must match world_label bit-for-bit, v3 sort-only, v4 cursor-containment); WorldLabelRank duplication now extended to Pixel/Icon/Textured bars + damage popups; only nameplate anchors remain single-instance
- [Panel input blocking](panel_input_blocking.md) — "no click-through" needs TWO mechanisms: UI focus path (ui_focus_system→Interaction→button_system, NOT picking backend) vs direct-input world path; backdrop blocks because scene-spawned+laid-out, imperative blocker didn't because action-time spawn races layout; overlay backdrop's own FocusPolicy::Block+Interaction gap now fixed too
- [Targeting CurrentTarget mirror](targeting_currenttarget_mirror.md) — CurrentTarget = primary player's PlayerTarget (one-directional mirror); SetTarget/ClearTarget divergence (ring+auto-clear regression) now FIXED — both route through apply_player_target/clear_player_target; camera→owner resolution robust across all modes
- [parse_key case-sensitivity trap](parse_key_case_sensitivity_trap.md) — parse_key was uppercase-only for letters; single-lowercase-letter normalization CONFIRMED present on main (8+ shared callers, widening-only change)
- [Action-bar per-player keying](action_bar_per_player_keying.md) — owner_player + filter-loop rewrite; intent/cooldown resources still keyed by slot_key alone → disjoint-keys invariant guarded by scene-load warn + CLI error, both bar.id-dependent (dup id = blind spot)
- [Stat ownership: global vs instance](stat_ownership_global_vs_instance.md) — LoadedStats resource vs per-entity StatMap; per-player costs via stat_templates on player w/ zero new schema; primitive players now get StatMap too (player_model_source_unification fixed the old site-2 gap); check/deduct single-decision footgun
- [interactable vs targeting player-scope](interactable_vs_targeting_player_scope.md) — interactable_system NOW per-player (fixed 847695b); residual global-scope quirks: panels_open kills all players' interact, attack_missed emitted globally
- [RenderLayers reserved scheme](render_layers_reserved_scheme.md) — layer-restricted meshes drop out of layer-0 lights' shadow passes (unlit-only scheme); heap-free below layer 64; ring_layer_for_player()/all_ring_layers() now the single owning helpers (old "no owning module" gap fixed)
- [Scene-load resource threading](scene_load_resource_threading.md) — why deferred insert_resource at scene load is never stale (sync point + same-command-queue atomicity), and the unordered terrain spawn path
- [Gamepad input pattern](gamepad_input_pattern.md) — BoundGamepad bind-once replaced resolve_gamepad (deleted); TWO writers not one (spawn path bypasses `claimed`); camera resolves via CameraTargets (terminology updated post camera_modes v1/v2 — OrbitCamera/PartyOrbitCamera no longer exist, replaced by ActiveCameraMode enum); WASM press-order pad swap; hot-join out-of-band pad identity; headless gamepad test harness now exists (gamepad_binding_tests.rs)
- [Mouse-wheel normalization](mouse_wheel_input_normalization.md) — winit facts: Windows Line is ALWAYS ±1.0/notch, web Pixel = deltaY×devicePixelRatio (~100-150/notch in Chrome); unit-normalization fix SHIPPED as normalized_wheel_delta (per-frame notch cap incl.); the separate dt-scaling/impulse-vs-continuous design bug remains open
- [Ground detection & jump invariants](ground_detection_jump_invariants.md) — detach needs apex > 0.7m; raw_grounded vs coyote-buffered is_grounded split; no Default on CharacterController is correct; two physics clocks
- [Airborne ground re-acquisition](airborne_ground_reacquisition.md) — 0.7m proximity sensor grabs a NEW platform mid-ascent; tick-18 = grace expiry not cube; jump_exit `>=` priority swap is the visible glitch; Godot/UE/Quake/KCC precedent
- [Wall-friction velocity crush](wall_friction_velocity_crush.md) — solver-side 3rd controller bug: Δv_y = μ·v_cmd per step; NPCs immune at μ=0; landed fix's two load-bearing rapier facts; idle-creep drift ratio = 1−μ/tanθ and the idle_drag confound
- [Spawn id lifecycle invariants](spawn_id_lifecycle_invariants.md) — Spawn captures Transform at action time (despawn-safe); registry id reuse orphans entities; Despawn warns on missing id; container.looted skipped when empty
- [Delayed-event staleness class](delayed_event_staleness_class.md) — no cancellation, never cleared on LoadScene, and dropped if the FSM left the state handling it (use global_on); initial_items is spawn-only
- [Ground cast penetrating normal](ground_cast_penetrating_normal.md) — normal1 is an EPA MTV not a surface normal; single-nearest-hit floor-veto class (sensor veto fixed via exclude_sensors, solid-geometry veto now also FIXED via ground_cast()'s bounded re-query loop); witness1 world-space chain fully traced; QueryFilter predicate ANDs; underfoot-tolerance ⇒ 60° slope ceiling
- [WorldLabel cleanup & at_entity](world_label_cleanup_and_at_entity.md) — two overlapping owners for WorldLabel widget teardown; at_entity copies full GlobalTransform incl. scale
- [UI Label/Button box overflow reliance](ui_label_box_overflow_reliance.md) — ~34 shipped defs render text outside their fixed `size:` box on purpose; unconditional `Overflow::clip()` deletes it. Bevy clip cost/hit-test facts
- [UI flat-scan blindness & taffy facts](ui_flat_scan_and_taffy_facts.md) — `scene.ui` scanned flat in 11 places (radar pre-pass is a *functional* dep); verified Bevy 0.18/taffy absolute/overflow/gap/justify semantics
- [UI trigger source enumeration](ui_trigger_source_enumeration.md) — the 6 authored + 6 engine-generated `ui.button_pressed:` sources; forward vs reverse checks have opposite failure-mode polarity; `{self}`/`dialogue_choice` false-positive traps
- [CLI validate coverage model](cli_validate_coverage_model.md) — checks vanish silently when a catalog is missing; out-of-convention scenes now parsed but covered-set duplicates diagnostics; SIX Action surfaces not five (ActionSlotDef grep-invisible + unwalked); assets.ron still case-blind
- [Schema-tightening blast radius](schema_tightening_blast_radius.md) — deny_unknown_fields turns a silent field drop into a whole-FILE failure; 3 of 5 Action-bearing loaders have no Failed arm; Action's parent structs still permissive
- [CLI/runtime mirror check pairs](cli_runtime_mirror_check_pairs.md) — the validate-check + scene-load-warn! pattern: duplicated predicates belong in schema/; work out which band is WIDER (CLI ⊇ runtime = runtime is noisier); watch for clamps/default changes riding along; a shared helper's *remedy* text is often true at only one call site (validate_camera_mode_def)
- [Demo project baseline determinism](demo_project_baseline_determinism.md) — test_web.py globs EVERY scene for a 4%-threshold baseline; NON_DETERMINISTIC_SCENES exclusion mechanism now exists (use as last resort — prefer settling content first)
