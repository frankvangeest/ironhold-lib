(
    schema_version: 2,
    rules: [
        (
            on: "scene.ready:main",
            do_actions: [ LoadScene("scenes/does_not_exist.scene.ron") ],
        ),
        (
            on: "ui.button_pressed:pause",
            do_actions: [ ToggleOverlay("scenes/also_does_not_exist.scene.ron") ],
        ),
    ],
)
