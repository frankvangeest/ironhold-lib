(
    schema_version: 2,
    rules: [
        (
            on: "scene.ready:main",
            do_actions: [ LoadScene("../load_scene_path_traversal_sibling/evil_scenes/thing.scene.ron") ],
        ),
    ],
)
