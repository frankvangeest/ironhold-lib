(
    schema_version: 2,
    rules: [
        (
            on: "scene.ready:main",
            do_actions: [
                SpawnEffect(key: "hit_spark"),
                PlaySound(key: "click"),
            ],
        ),
    ],
)
