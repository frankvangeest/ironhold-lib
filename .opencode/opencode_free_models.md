# OpenCode Free Models Catalog

> **Last generated**: 2026-09-17
> **Corrections applied**: 2026-09-22, per `planning/features/opencode_compatibility.md` finding
> F8 (verified against live OpenRouter/Zen model lists and models.dev) and F10 (Gemini).
> **Known-limitations research pass added 2026-09-23** (see "Known Limitations by Model" below) —
> triggered by a real `cohere/north-mini-code:free` failure that benchmark ratings alone didn't
> predict. This is a catalog for reference/regeneration — the routing decisions actually in effect
> for this repo live in `.opencode/opencode.json` and `.opencode/README.md`, not here.
>
> **Sources**:
> - OpenCode Zen: `https://opencode.ai/zen/v1/models` (fetched via API)
> - OpenRouter Free Models: `https://openrouter.ai/collections/free-models` (scraped)
> - Google Gemini pricing: `https://ai.google.dev/gemini-api/docs/pricing`
> 
> **Purpose**: Provide a curated list of free AI models available for use with OpenCode, rated for various agent roles and tasks. This list can be regenerated by re-running the source fetches.

---

## How This List Was Made

### 1. OpenCode Zen Models
- Fetched via: `GET https://opencode.ai/zen/v1/models`
- The API returns all models in the OpenCode Zen catalog
- Models with "free" in the `id` field or owned by "opencode" are included
- File: `opencode_zen_models.txt` (binary JSON saved from PowerShell fetch)

### 2. OpenRouter Free Models
- Scraped from: `https://openrouter.ai/collections/free-models`
- Parsed model cards from the HTML page
- Each model entry includes: provider/name, context window, token quota, ranking position, and use case description
- File: `openrouter_free_models.html` (or could be parsed programmatically)

### 3. Rating Criteria
Models were rated across these agent task categories:
| Category | Description |
|---|---|
| **Coding** | Code generation, debugging, refactoring, agentic software engineering |
| **Reasoning** | Multi-step logic, planning, mathematical reasoning |
| **Analysis** | Code review, documentation, pattern analysis |
| **Exploration** | Codebase exploration, grep/grep equivalents, read-only tasks |
| **Creative** | Writing, brainstorming, design alternatives |
| **Budget** | Cost sensitivity (all are "free" on OpenRouter, but may have different Zen access) |

### 4. Rating Scale
- ⭐⭐⭐⭐⭐ **Excellent** - Top-tier for this task, comparable to paid models
- ⭐⭐⭐⭐ **Very Good** - Strong performer, may have limitations
- ⭐⭐⭐ **Good** - Usable for most cases, some caveats
- ⭐⭐ **Fair** - Works for simple tasks, avoid complex work
- ⭐ **Poor** - Avoid for this task category

### 5. Known-Issue Research (added 2026-09-23, mandatory going forward)

Real incident: `cohere/north-mini-code:free` was rated highly on benchmarks and Apache-2.0
licensing alone, then failed in live use — it stopped making real edits mid-task and started
re-describing its plan instead, a documented model-card behavior (dropped reasoning state between
tool calls) that a benchmark score alone never would have surfaced. **A star rating from
benchmarks is not sufficient to route a model to an unsupervised agentic role.** Before adding or
re-routing any model, research (WebSearch) each of the following and record a SAFE / CAUTION /
AVOID verdict with sources in "Known Limitations by Model" below:

- Model-card language about reasoning-state/context preservation across tool calls or turns
- Documented fabrication/hallucination tendency under iterative or long multi-turn pressure
- Whether tool/function-calling is confirmed supported by the model card **and** by the serving
  endpoint actually used (OpenRouter vs. OpenCode Zen vs. native SDK can differ)
- Whether headline benchmark numbers require a non-default setting (e.g. "max thinking") to
  achieve, and what the number is without it
- Independent (non-vendor) benchmark reproduction, vs. self-reported-only numbers
- Free-tier rate limits/availability — a model with a sub-90% success rate on its own free
  endpoint can look like a silent task failure rather than an obvious rate-limit error
- Real advertised-vs-practically-usable context window (community reports, not just the spec sheet)

---

## OpenCode Zen Free Models

Fetched from `https://opencode.ai/zen/v1/models` on 2026-09-17. These are models owned/named by the OpenCode project itself.

| Model ID | Type | Context | Rating |
|---|---|---|---|
| `nemotron-3-ultra-free` | LLM | Not specified | ⭐⭐⭐⭐ Coding: ⭐⭐⭐⭐, Reasoning: ⭐⭐⭐⭐ |
| `nemotron-3.5-lightning-free` | LLM | Not specified | ⭐⭐⭐⭐ Coding: ⭐⭐⭐⭐, Reasoning: ⭐⭐⭐⭐ |
| `muse-spark-1.3-contributor-free` | LLM | Not specified | ⭐⭐⭐ Reasoning: ⭐⭐⭐ |
| `muse-spark-1.2-contributor-free` | LLM | Not specified | ⭐⭐⭐ Reasoning: ⭐⭐⭐ |
| `mimo-v2.5-free` | LLM | Not specified | ⭐⭐ Reasoning: ⭐⭐ |
| `ling-3.0-flash-fin-free` | LLM | Not specified | ⭐⭐⭐⭐ Reasoning: ⭐⭐⭐⭐ |
| `deepseek-v4-flash-free` (added 2026-09-22) | LLM | 262K | ⭐⭐⭐ Fast, capable flash-class model; good for running a CLI and summarising output, not deep reasoning |
| `jev-1.13-free` (added 2026-09-22) | LLM | Not specified | Not yet rated for this repo |
| `mimo-v2.6-flash-free` (added 2026-09-22) | LLM | Not specified | Not yet rated for this repo |

Zen also lists the **paid** `deepseek-v4.1-flash` (not `-free`) — this is a different, weaker model
than OpenRouter's paid `openrouter/deepseek/deepseek-v4.1-flash` in the escalation tier below;
don't confuse the two by name alone.

> **Note**: OpenCode Zen models are accessed directly; no API key needed if running OpenCode locally with the Zen backend. Zen's free models are offered as time-limited promotions and can disappear (this happened to OpenRouter's Union Alpha within 5 days of it being catalogued — see below) — re-verify before depending on one long-term.

---

## OpenRouter Free Models

Fetched from `https://openrouter.ai/collections/free-models` on 2026-09-17. These models are free on OpenRouter (may require account signup, have rate limits, or usage caps).

| Model | Provider | Context | Cost | Best For |
|---|---|---|---|---|
| **Nemotron 3 Ultra** (`nvidia/nemotron-3-ultra-550b-a55b:free`) | NVIDIA | 1M tokens | $0/M | Agent orchestration, coding agents, deep research, complex enterprise tasks; multi-step reasoning and planning |
| **Laguna S 2.1** (`poolside/laguna-s-2.1:free`) | Poolside | 262K tokens | $0/M | **Coding** - strongest coding model in its category; 70.2% Terminal-Bench 2.1; agentic software engineering |
| **Ling 3.0 Flash Fin** (`inclusionai/ling-3.0-flash-fin:free`) | inclusionAI | 262K tokens | $0/M | Finance-focused; complex multi-step tasks, long-horizon planning, investment workflows |
| **Nemotron 3.5 Lightning** (`nvidia/nemotron-3.5-lightning:free`) | NVIDIA | 1M tokens | $0/M | High-throughput agentic workloads; domain-specific customization; MoE with 3B active / 30B total |
| **Ling 3.0 Flash VL** (`inclusionai/ling-3.0-flash-vl:free`) | inclusionAI | 262K tokens | $0/M | Visual perception + text; tool calling; hybrid instant/reasoning model |
| **Inkling** (`thinkingmachines/inkling:free`) | Thinking Machines | 1.05M tokens | $0/M | General-purpose reasoning, coding, agentic systems, tool-use, multimodal (image+audio) |
| **Nemotron 3 Super** (`nvidia/nemotron-3-super-120b-a12b:free`) | NVIDIA | 262K tokens | $0/M | 120B MoE model (12B active); 50%+ higher token generation; 1M token context; RL-trained |
| **Ling 3.0 Flash Sante** (`inclusionai/ling-3.0-flash-sante:free`) | inclusionAI | 262K tokens | $0/M | Medicine/health-focused; clinical safety, evidence-based retrieval |
| **Inkling Small** (`thinkingmachines/inkling-small:free`) | Thinking Machines | 262K tokens | $0/M | Efficient Inkling variant; 12B active / 276B total; reasoning, coding, agentic workflows |
| **Nex-N2.5-Pro** (`nex-agi/nex-n2.5-pro:free`) | Nex AGI | 262K tokens | $0/M | **Coding/Engineering** - agentic coding with visual feedback loop; explore codebases, implement changes, run commands, browser automation. **Used in this repo's routing** for `integration-test-author`, added 2026-09-23 specifically to be on a different provider than Poolside (see "C tier is split across providers" in `.opencode/README.md`). |
| **Nex-N2.5-Mini** (`nex-agi/nex-n2.5-mini:free`) | Nex AGI | 262K tokens | $0/M | Smaller Nex-N2.5; same agentic coding capabilities, compact footprint. **Used in this repo's routing** for `data-format-doc-writer`, added 2026-09-23. |
| **North Mini Code** (`cohere/north-mini-code:free`) | Cohere | 256K tokens | $0/M | **Coding** - agentic coding model; 3B active / 30B MoE; optimized for OpenCode/SWE-Agent; Apache 2.0 open-weight. **Tried and reverted, 2026-09-23** — briefly used as the default `build`/`general` model after `poolside/laguna-s-2.1:free` hit an upstream rate limit, but stopped making real edits mid-implementation and started re-describing the plan instead of acting on it. Matches two documented issues on Cohere's own model card: it requires preserving reasoning content between tool calls or the model "reconstruct[s] its plan on the next turn" (needs Cohere's own "Melody" parser for this in local serving; unverified whether a generic OpenRouter-fronted harness preserves it correctly), and independent review found it can "multiply fabricated completions under the pressure of a loop." **Not used anywhere in this repo's routing.** Don't re-add it as a primary implementation driver without first confirming the reasoning-state issue doesn't apply to however it's being invoked. |
| **Laguna XS 2.1** (`poolside/laguna-xs-2.1:free`) | Poolside | 262K tokens | $0/M | Compact coding agent; FP8 quantized; 256K context / 32K output tokens |
| **rerank-2.5-lite** (`voyageai/rerank-2.5-lite`) | Voyage AI | 32K tokens | $0.02/M tokens | **Retrieval** - reranker for improving search accuracy; 7.16% better than Cohere v3.5 |

**Removed 2026-09-22:** `stealth/union-alpha` — gone from OpenRouter (disappeared within ~5 days of
being added to this catalog; a live example of the "free endpoints churn" risk documented in the
compatibility plan).

**Excluded, do not route to:** `z-ai/glm-5.2:free` does **not** support tool calling on OpenRouter
— unusable for any agent role in this repo, all of which need tools.

**Corrected context values (2026-09-22):** Ling 3.0 Flash Sante and Inkling Small were previously
listed as "355B tokens" / "137B tokens" — both typos for a token-count where a context-window size
was meant; the real context window for both is 262K, now fixed above.

### Paid escalation model (not free — used deliberately, on request only)

| Model | Provider | Context | Cost | Used for |
|---|---|---|---|---|
| `deepseek/deepseek-v4.1-flash` | OpenRouter | 1M tokens | $0.15/M input, $0.60/M output | The sole paid tier in this repo's routing (`.opencode/opencode.json`'s `system-architect-deep`/`debug-detective-deep`) — deep root-causing and architectural tradeoff calls that a free model isn't trusted with. Reachable only via an explicit `-deep` @mention; never auto-delegated. |

Don't confuse this with OpenCode Zen's own **free** `deepseek-v4-flash-free` (different, lighter
model, listed above) — despite the similar name, they are not interchangeable; see
`planning/features/opencode_compatibility.md` Decision 4.

### Google Gemini free tier (added 2026-09-22 — see finding F10)

Provider id `google/`. Reads `GEMINI_API_KEY`/`GOOGLE_GENERATIVE_AI_API_KEY`/`GOOGLE_API_KEY`, or a
key from `opencode auth login`. Per `ai.google.dev/gemini-api/docs/pricing` on 2026-09-22, these
text models have a free tier **only on a Google Cloud project with no billing account linked**:

| Model | Notes |
|---|---|
| `google/gemini-3.8-flash` | Newest; Google pitches it for long-horizon software engineering and autonomous agents; 1M context. **Used in this repo's routing** for `ux-gamedesigner-reviewer`/`game-world-designer`. |
| `google/gemini-3.7-flash`, `-3.6-flash`, `-3.5-flash` | Older flash tiers, also free |
| `google/gemini-3.5-flash-lite`, `-3.1-flash-lite` | Lighter/cheaper variants |
| `google/gemini-2.5-pro`, `-2.5-flash`, `-2.5-flash-lite` | Previous generation, also free |
| `google/gemini-flash-latest` | Rolling alias |

`google/gemini-3.1-pro-preview` has **no** free tier — do not route anything to it without
confirming billing is intended.

If billing turns out to be enabled on the linked project, every Gemini call is billed
(`gemini-3.8-flash`: $0.75/M input, $3.75/M output as of 2026-09-22, ~5x the DeepSeek escalation
tier's cost, doubling 2026-12-31) — check this in AI Studio before relying on Gemini being free.
Gemini's free-tier daily request caps are low (historically tens to low hundreds per day per
model, shown per-project in AI Studio rather than in the docs) — route only low-volume agents to
it, never a per-command or always-on review slot.

---

## Known Limitations by Model (research pass 2026-09-23)

Triggered by the `cohere/north-mini-code:free` incident (see its catalog entry above and
`.opencode/README.md`) — benchmark scores didn't predict that failure, so every model actually in
this repo's routing got the same scrutiny applied after the fact, via three parallel research
passes. See "Known-Issue Research" above for the methodology; re-run it for any new model before
routing it to a role. **None of the models below rose to an outright AVOID** on current evidence —
none has a documented "stops editing, reconstructs its plan" analog as blatant as the Cohere
incident — but several share the same *category* of risk (reasoning-state loss, vendor-only
benchmarks, tool-call plumbing immaturity) and are flagged CAUTION accordingly. These star ratings
in the tables above and below reflect benchmark/capability performance only — read this section
before routing any model to an unsupervised agentic role.

### Primary code-editing models

**`poolside/laguna-s-2.1:free`** (default `build`/`general` agent, `ron-gameplay-scripter`) —
**CAUTION**. The headline 70.2% Terminal-Bench 2.1 / 40.4% DeepSWE numbers are reported only with
"max thinking" enabled (Poolside's own "Introducing Laguna S 2.1" post); without it they drop to
60.4% / 16.5%. Unconfirmed whether OpenRouter's free endpoint (as OpenCode calls it, with no
thinking-effort parameter set) defaults to max thinking or something lower — real day-to-day
capability may sit closer to the lower numbers. Model card also documents "harness overfitting"
(relies on memory of Poolside's native tool schema; can emit invalid calls against a
differently-shaped harness schema) and occasional malformed JSON on array-typed tool arguments —
both typically self-correct via a harness retry rather than silently abandoning the edit, a
materially milder failure mode than north-mini-code's. Model card also admits quality degrades at
long context despite the advertised up-to-1M window. No evidence found of the "stops editing,
re-describes the plan" failure mode specifically — this is the closest thing to a safe pick in the
primary coding role, just not risk-free.

**`poolside/laguna-xs-2.1:free`** (unused, catalogued candidate) — **CAUTION**. Terminal-Bench 2.1
only ~33-38% (vs. S 2.1's 70.2%) — a big capability gap for sustained agentic/terminal work despite
similar SWE-bench-style scores. Model card explicitly documents "native reasoning with interleaved
thinking between tool calls" — structurally the same reasoning-across-tool-calls dependency that
caused the north-mini-code incident elsewhere. Not proven broken here, but don't promote this to
active routing without testing that dependency directly first.

**`nex-agi/nex-n2.5-pro:free`** (`integration-test-author`) — **CAUTION**. Tool-calling and
structured output are solid, but every benchmark claim (82.7 Terminal-Bench 2.1, 61.2 SWE-Bench
Pro) is vendor-self-reported via Nex-AGI's own harness, flagged as such by third-party coverage —
no independent reproduction found. Launched 2026-09-08, essentially no independent agentic-coding
track record yet. Risk here is role-specific: wrong-but-plausible test code is easier to miss in
review than wrong prose.

**`nex-agi/nex-n2.5-mini:free`** (`data-format-doc-writer`) — **CAUTION**, slightly better signal
than Pro. Same vendor-only-benchmark caveat, but one independent hands-on review found it declines
low-confidence answers rather than fabricating — "a meaningfully better failure mode than
confident hallucination" (not tested on sustained code editing). Role here (single-shot markdown
doc writing) is lower-stakes than Pro's — bad docs are easy to spot-check.

**`cohere/north-mini-code:free`** — **AVOID**. See dedicated entry above; not used in routing.

### Review/reasoning models (no edit permission — risk is fabricated findings, not broken edits)

**`nvidia/nemotron-3-ultra-550b-a55b:free`** (`system-architect`, `debug-detective`, plus the
`opencode/nemotron-3-ultra-free` Zen twin backing the built-in `plan` agent and
`/code-review`/`/plan-review`/`/ship` orchestration) — **CAUTION**. NVIDIA's own tech report
documents that reasoning tokens from previous turns are dropped whenever a new user/tool-result
message arrives — a different trigger than Cohere's "between tool calls" case, but the same
failure family (reasoning-state loss → risk of reconstructing/fabricating rather than continuing
coherently). Separately, OpenRouter's own 3-day snapshot (2026-09-19) measured only 84.07%
successful-response rate on the free endpoint (100% reachable, but ~16% of calls errored or
returned empty) — free tier capped at 20 RPM/200 RPD with no SLA. For a review agent this could
look like a dropped/incomplete review rather than an obvious error — worth a second look if a
review agent ever comes back suspiciously thin.

**`nvidia/nemotron-3.5-lightning:free`** (`small_model`, `/query` `/validate` `/wasm-dev`
`/rust-docs`) — **CAUTION**. No `response_format`/enforced JSON schema support. Advertised 1M
context vs. NVIDIA's own docs citing a native 262K window, with community reports of materially
lower practical usable context on top of that — a real advertised-vs-actual gap. Smallest model in
the family (3B active) increases fabrication risk beyond its current scope of short, low-stakes
commands — don't extend it to longer review work.

**`nvidia/nemotron-3-super-120b-a12b:free`** (`wasm-perf-reviewer`) — **CAUTION**. Tool-calling and
structured JSON output are better-specified than Lightning's, but HuggingFace discussions on a
sibling Nemotron-3 model document broken tool-call-argument parsing and reasoning content leaking
into the message body on certain serving stacks (version-dependent; NVIDIA's own workaround was
"parse the arguments yourself") — a family-wide plumbing-immaturity signal, not confirmed on this
exact OpenRouter/Zen endpoint. Worth spot-checking that its tool calls parse cleanly rather than
assuming they do.

### Exploration / design-consultation models

**`thinkingmachines/inkling-small:free`** (built-in `explore` agent, read-only) — **CAUTION**.
Model card itself lists hallucination, imprecise instruction-following, and degraded
long-multi-turn performance as known limitations — the same *category* of issue as
north-mini-code, without the specific tool-call mechanism. Its Terminal-Bench 2.1 score is
separately flagged by the vendor's own model card as partially contaminated by web-search-derived
solutions scoring 0 — a benchmark data-integrity flag, not a runtime risk given this role has no
edit permission.

**`thinkingmachines/inkling:free`** (full, unused, catalogued candidate) — **CAUTION**, same
documented limitations as Inkling Small (larger model, no evidence scale fixes the pattern).

**`google/gemini-3.8-flash`** (`ux-gamedesigner-reviewer`, `game-world-designer`) — **SAFE** for
tool-calling itself (confirmed supported, no agentic-loop fabrication reports found), but
**CAUTION** on free-tier throughput: Google's own docs cap no-billing access at ~10-15 requests/
minute — an OpenCode agentic loop making rapid tool-call round-trips could hit 429s mid-task. Not
a correctness risk, but expect occasional stalls, not silent wrong output.

### Not yet re-verified against this methodology

`inclusionai/ling-3.0-flash-*` variants, `mimo-v2.5/2.6-flash-free`, `muse-spark-1.2/1.3-free`,
`jev-1.13-free`, and `deepseek-v4-flash-free` (Zen) are catalogued but currently unused in
routing — apply the "Known-Issue Research" step above before activating any of them.

---

## Model Ratings by Agent Role/Task

> Ratings below are benchmark/capability scores only — see "Known Limitations by Model" above for
> the reliability caveat on each currently-routed model before trusting a rating alone.

### 🎯 Coding Agents
| Model | Rating | Notes |
|---|---|---|
| `poolside/laguna-s-2.1:free` | ⭐⭐⭐⭐⭐ | **Top pick** - strongest coding benchmark scores; used in this repo for `integration-test-author`/`ron-gameplay-scripter`/`data-format-doc-writer` and the default `build`/`general` agents |
| `cohere/north-mini-code:free` | ⭐⭐⭐⭐ | Good for OpenCode integration; Apache 2.0 licensed |
| `poolside/laguna-xs-2.1:free` | ⭐⭐⭐⭐ | Compact coding; FP8 quantized; good for simpler tasks |

_(Corrected 2026-09-22: this table previously listed `poolside/laguna-s-2.1:free` twice.)_

### 🧠 Reasoning & Planning
| Model | Rating | Notes |
|---|---|---|
| `nvidia/nemotron-3-ultra-550b-a55b:free` | ⭐⭐⭐⭐⭐ | Best for multi-step reasoning, planning, agent orchestration |
| `nvidia/nemotron-3-super-120b-a12b:free` | ⭐⭐⭐⭐⭐ | Excellent; 1M token context; multi-agent applications |
| `thinkingmachines/inkling:free` | ⭐⭐⭐⭐ | Strong general-purpose reasoning; multimodal support |
| `nvidia/nemotron-3.5-lightning:free` | ⭐⭐⭐⭐ | Good for high-throughput agentic workloads |

### 📊 Analysis & Code Review
| Model | Rating | Notes |
|---|---|---|
| `voyageai/rerank-2.5-lite` | ⭐⭐⭐⭐ | For retrieval-augmented analysis, not direct code review |
| `inclusionai/ling-3.0-flash-fin:free` | ⭐⭐⭐⭐ | Finance-focused analysis; complex multi-step tasks |

**Note (corrected 2026-09-22):** this table previously suggested `anthropic/claude-sonnet-4-5` as
the top pick for code review here. That's true, but it isn't an OpenCode routing option — this
repo's decision (`planning/features/opencode_compatibility.md` Decision 1) is that the mandatory
code review stays a **Claude Code** `/code-review`, run outside OpenCode entirely, not an
OpenCode agent pointed at an Anthropic model. No `anthropic/*` model appears anywhere in
`.opencode/opencode.json`.

### 🌐 Exploration & Read-Only Tasks
| Model | Rating | Notes |
|---|---|---|
| `thinkingmachines/inkling-small:free` | ⭐⭐⭐⭐ | Efficient; good for codebase exploration |
| `inclusionai/ling-3.0-flash-vl:free` | ⭐⭐⭐⭐ | With visual perception for UI/code analysis |
| `muse-spark-1.3-contributor-free` | ⭐⭐⭐ | Basic exploration capabilities |

### 💰 Budget-Sensitive Tasks
| Model | Cost | Rating |
|---|---|---|
| All OpenRouter models listed above | $0/M (free) | Varies by model |
| OpenCode Zen free models | Free (local) | See individual ratings |

---

## Regenerating This List

To regenerate this list when free models change:

### Option A: Manual Re-fetch
```bash
# 1. Fetch OpenCode Zen models
curl -s https://opencode.ai/zen/v1/models > opencode_zen_models.json

# 2. Fetch OpenRouter free models (requires parsing HTML)
# Use a script or browser to scrape https://openrouter.ai/collections/free-models
# Save the relevant model cards

# 3. Update this markdown file with new model data
# Re-rate models as needed
```

### Option B: Automated Script (Python example)
```python
import requests
import json

# Fetch OpenCode Zen
zen_resp = requests.get("https://opencode.ai/zen/v1_models")
zen_models = zen_resp.json()

# Filter for free models
free_zen = [m for m in zen_models if "free" in m.get("id", "").lower()]

# Fetch OpenRouter (would need HTML parsing or API)
# OR free list is at: https://openrouter.ai/api/v1/models?free=true

# Generate markdown...
```

### Option C: Update Script Included
- Add a script to `.opencode/scripts/` that fetches both sources
- Run `python .opencode/scripts/update_free_models.py`
- Overwrites `.opencode/opencode_free_models.md`

---

## Agent Configuration Recommendations

**These example blocks were invalid config as written (fixed 2026-09-22):** `"agents"` is not a
real `opencode.json` key — the correct key is singular `"agent"`. A top-level `"model_routing"`
key doesn't exist either; per-task routing is expressed by setting `model` on each individual
`agent`/`command` entry, not a separate routing table. `openrouter/mistral/mistral-7b` isn't a
model in this catalog. The real, working config for this project is `.opencode/opencode.json`
(see `.opencode/README.md` for the human-readable summary) — the block below is a minimal,
corrected illustration of the mechanism, not a second source of truth to keep in sync with it.

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "model": "openrouter/poolside/laguna-s-2.1:free",     // free default for the build/general agents
  "small_model": "opencode/nemotron-3.5-lightning-free", // free, used for background titles

  "agent": {
    "code-review": {
      "mode": "subagent",
      "model": "cohere/north-mini-code:free",  // free coding model for reviews
      "description": "Reviews code for correctness and best practices"
    },
    "exploration": {
      "mode": "subagent",
      "model": "thinkingmachines/inkling-small:free",  // free exploration
      "description": "Explores codebase and finds relevant files"
    }
    // no anthropic/* model belongs here — Claude Code, not OpenCode, is where those run.
  }
}
```

---

## Model Availability Notes

1. **OpenRouter free models**: May have rate limits, daily quotas, or require account signup. Check OpenRouter status for current availability.

2. **OpenCode Zen free models**: Available when running OpenCode with Zen backend; no external API keys needed.

3. **Model variants**: Some models have `#high`, `#fast`, or other variants specified after `#` in the model ID.

4. **Context windows**: Vary significantly; check each model's specific limits for long codebase analysis.

5. **Licensing**: OpenRouter models follow their respective provider licenses; OpenCode Zen models are for local/OpenCode usage.

6. **Rate limiting**: Free models may have stricter rate limits than paid tiers; batch operations may be limited.

---
*This catalog is a living document. Re-fetch sources and regenerate as needed to keep model availability current.*